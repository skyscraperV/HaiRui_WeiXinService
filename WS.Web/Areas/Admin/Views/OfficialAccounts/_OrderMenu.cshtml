@model IEnumerable<WS.Model.OfficialAccounts>
@foreach (var item in Model)
{
    <li>
        @Html.ActionLink(item.WeiXinName, "List", "Orders", new { Area = "Admin", AccountID = item.AccountID }, null)
    </li>
}
