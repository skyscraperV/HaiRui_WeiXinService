@model IEnumerable<WS.Model.OfficialAccounts>
@foreach (var item in Model)
{
    <li>
        @Html.ActionLink(item.WeiXinName, "List", "Subscribes", new { Area = "Admin", AccountID = item.AccountID }, null)
    </li>
}
